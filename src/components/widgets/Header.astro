---
import { Button } from "@/components/ui/button";
---

<header>
  <div class="mx-auto max-w-screen-lg px-4 py-8 sm:px-6 sm:py-12 lg:px-8">
    <div
      class="flex items-start justify-between gap-2 sm:gap-4 md:items-center"
    >
      <a href="/" class="logo flex items-center">
        <figure>
          <!-- <Image
            src={Logo}
            alt={config.site.title}
            width={Logo.width / 2}
            height={Logo.height / 2}
            class="rounded cursor-pointer"
            decoding="async"
            loading="lazy"
          /> -->
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            stroke="currentColor"
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            class="w-10 h-10 text-white p-2 bg-indigo-500 rounded-full"
            viewBox="0 0 24 24"
          >
            <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"
            ></path>
          </svg>
          <span class="sr-only">Anish Shobith P S's logo</span>
        </figure>
      </a>
      <ul class="ml-auto hidden items-center gap-48 md:flex lg:flex">
        <Button>Login</Button>
      </ul>
    </div>>
  </div>
</header>
